<div class="container">
  <div class="row">
    <div class="col">
      <h4>商品一覧</h4>
    </div>
  </div>

  <table>
    <thead class="table-active">
      <tr>
        <th>商品ID</th>
        <th>商品名</th>
        <th>税抜価格</th>
        <th>ジャンル</th>
        <th>ステータス</th>
      </tr>
    </thead>
    <% @items.each do |item| %>
    <tbody class="table table-borderless">
      <tr>
        <td><%= item.id %></td>
        <td><%= item.name %></td>
        <td><%= item.price %></td>
        <td><%= item.genre.name %></td>
        <td><%= item.is_active %></td>
      </tr>
    </tbody>
    <% end %>
  </table>
</div>

